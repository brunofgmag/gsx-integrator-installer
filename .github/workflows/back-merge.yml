name: Back-merge

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: back-merge-main
  cancel-in-progress: false

jobs:
  back-merge:
    uses: brunofgmag/gsx-integrator-ci/.github/workflows/back-merge.yml@main
    secrets: inherit
